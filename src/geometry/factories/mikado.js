// TODO: implement mikado factory — derives all geometry from sideLength + patternParams
export function buildMikado(sideLength, stripProperties) {
  throw new Error('Factory not yet implemented — use JSON directly');
}
