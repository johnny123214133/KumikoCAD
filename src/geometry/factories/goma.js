// TODO: implement goma factory — derives all geometry from sideLength + patternParams.spacing
export function buildGoma(sideLength, patternParams, stripProperties) {
  throw new Error('Factory not yet implemented — use JSON directly');
}
